import React from 'react';
import Search from '../Search';

import {
    Container
} from './styled';

export default () =>{
    return (
        <Container>
            <Search />            
        </Container>
    );
}